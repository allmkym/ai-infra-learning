# Modern C++ 教学计划：起点诊断后的修订版

本文件保存课程安排；实际教学位置、暂停状态及下一步以 HANDOFF.md 为准。

**总体安排：继续以 CS106L 2026 春季语言主干为骨架，提前穿插工程工具，补充接口契约、借用生命周期、并发和性能。预计约 8 周，每周 18—24 小时，包含讲解、查资料、编码、调试和复习。排期随验收调整。**

## 诊断结论与证据边界

- 已观察到：能借助文档组织小型 C++ 程序、拆分函数，使用 vector、文件流、异常、const 引用，并采用复制后排序的方式保留原数据。
- 已正确解释：string_view 按值传入不复制字符，vector 引用参数访问调用者容器，vector 按值参数的修改不影响调用者；返回 owning string 和悬空 string_view 的区别。
- 在提示后完成：编译错误修复、解析指针边界、全文件累计、空数据判定和一般空白处理。最终编译无警告，12 类功能用例通过。
- 需巩固：标准库头文件与返回对象的使用、接口前置条件和失败语义、合法空区间与非法范围的区别、构造语义和拷贝消除规则的精确表达。
- 尚未验证：完整初始化与类型推导规则、类设计和异常安全、模板独立编写、复杂借用失效、C++ 并发独立实现、内存模型和性能分析。未验证不等同于不会。
- 查 API 是允许且正常的工作方式，不以背诵签名评估能力；需要能解释查到的接口契约并在新场景正确使用。

## 与初版计划相比的调整

1. 熟悉的值/引用和 owning/borrowing 概念采用短讲与迁移练习，不再安排长时间从零重讲。
2. 保留初始化、类型系统和生命周期的完整覆盖，不因两个概念题正确而跳过整个基础阶段。
3. 增加接口契约训练：输入范围、返回值、失败表现、所有权和失效条件，并将这些要求落实到代码。
4. 练习不再局限于“补齐统计程序”，逐步增加自定义类型、多文件模块和需求变更。
5. 将高级泛型、PMR、复杂 ABI 和无锁算法放到后续扩展，不作为进入 CUDA 的统一门槛。
6. 不继续扩大起点测试范围。后续缺口通过正常教学和作业发现。

## 核心课程

| 阶段 | 系统覆盖的内容 | 主要实践与验收 |
| --- | --- | --- |
| 1. 初始化、类型与接口契约 | 常见初始化形式；auto、引用和 const；作用域与生命周期；基础值类别；按值/引用传参；类型转换与常见重载；头文件与返回结果的查阅方法 | 短函数和小实验，解释创建了什么对象、引用谁、何时失效；对 API 的输入、输出、失败和边界形成明确说明 |
| 2. 类、资源管理和对象操作 | 类的不变量、成员初始化、构造析构、explicit；Rule of Zero/Five；拷贝/移动构造与赋值；unique_ptr；shared_ptr/weak_ptr 的基本使用；异常路径；基本继承与虚析构 | 独立实现一个功能受限的资源管理类，如 FileDescriptor；正确处理移动、禁止或定义拷贝、失败和析构；比较使用标准成员实现 Rule of Zero 的设计 |
| 3. 容器、算法与实用泛型 | 常用容器的选择、复杂度和失效规则；迭代器与算法；lambda 捕获及生命周期；函数/类模板；常见类型推导；转发引用和 std::forward 的基础理解 | 小型数据处理模块及简单泛型函数；改变数据类型或需求后仍能修改实现；用反例检查迭代器和捕获失效 |
| 4. 视图、错误处理与工程组织 | span/string_view 的借用；参数/返回值接口设计；optional/variant 基本使用；异常传播与基本异常安全保证；编译链接、ODR、头文件依赖、target-based CMake、测试与 sanitizer | 完成一个带清楚所有权、失败语义和测试的多文件模块；能够区分编译、链接、生命周期和逻辑错误。构建调试从第一阶段就穿插，本阶段系统整理 |
| 5. 并发与基础内存模型 | 线程对象和线程生命周期；mutex、RAII 锁、条件变量、谓词与关闭协议；共享状态与不变量；data race、happens-before；atomics 的有限使用；relaxed/acquire/release/seq_cst 的基础案例 | 独立实现有界阻塞队列及使用它的程序，处理阻塞中的关闭和线程回收；解释同步关系；不要求实现无锁容器 |
| 6. 综合模块与性能验证 | 正确性基线、重复测量、热点分析；复制和分配成本；数据布局、cache 和 false sharing 的基本分析；代码审查与需求变更 | 以 GPT-2 项目中尚未独立实现的有限模块或新增功能为候选，形成串行正确性基线、必要的并发扩展和性能报告；不重写完整推理框架 |

第一阶段可适当压缩，类设计、对象操作与并发阶段应留出较多编码时间。不能机械地要求每个阶段恰好一周。

## 综合项目的范围约束

- 候选是同步线性算子执行模块：明确数据布局和输入约束，先串行正确，再按实际进展加入有限的并行执行。
- 也可根据前期练习暴露的问题，选择缓存或工作区管理模块；最终只做一个主要模块。
- 教師提供需求、接口约束和验收条件，学员设计和实现，再审查。
- 明确上游代码、原有参考实现和本次本人实现的边界。
- 不以采用最多新语法为目标；评估正确性、接口清楚程度、可调试性和测量证据。

## 后续扩展内容

简单 concepts、ranges 和 constexpr 可在对应模块介绍常见用法；以下内容在项目需要时深入：复杂 SFINAE、模板元编程、转发包装器、自定义 ranges/view、复杂 constexpr、PMR/allocator、高级 ABI、lock-free 和复杂原子算法。

基础使用 C++20；std::expected 等 C++23 内容明确标注为扩展，核对工具链后再使用。

## 每个单元的教学方式

1. 给出本单元目标及与前序知识的关系。
2. 系统讲解必要规则，并用小例子说明。
3. 进行引导分析，暴露理解差异。
4. 布置独立编码或修复任务，允许查 API，不提前提供完整实现。
5. 审查正确性、接口、失败路径与解释，再处理次要风格问题。
6. 延迟安排不同场景的迁移题，确认掌握而非记住答案。

某个熟悉点通过迁移题后压缩练习；不会因能口头复述就把整个主题视为掌握。遇到新语法先解释，不只给出要求机械套用的代码。

## 验收与记录

每个阶段需要至少一个独立实现或需求变更任务、必要的边界验证，以及对关键语义的解释。记录区分：理解讲解、提示后完成、独立完成、迁移通过。

查文档不扣分；把教师完整实现改写后提交不记为独立实现。运行结果与工具结果是证据，不替代语言规则和接口契约分析。

## 与 AI Infra 学习的衔接

Python/PyTorch 和算法保持并行。达到以下能力后即可开始 CUDA 基础，不必等完所有 C++ 扩展主题：

- 能独立编写并调试小型多文件程序。
- 正确使用容器、RAII、常见参数与返回值。
- 能阅读基础模板和常见推理代码。
- 能分析数据布局、生命周期和边界条件。

预计在前四阶段完成后评估这个转换点；若更早达到，也可并行开始。核心课完成代表形成继续学习和开发的基础，不代表精通整个 Modern C++。

CS106L 参考：https://web.stanford.edu/class/cs106l/lectures/
